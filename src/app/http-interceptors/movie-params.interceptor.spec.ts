import { MovieParamsInterceptor } from './movie-params.interceptor';

describe('MovieParams.Interceptor', () => {
  it('should create an instance', () => {
    expect(new MovieParamsInterceptor()).toBeTruthy();
  });
});
